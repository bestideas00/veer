
// vhano_box
new VenoBox({
    selector: '.my-image-links',
    numeration: true,
    infinigall: true,
    share: true,
    spinner: 'rotating-plane'
});

// counter_start
$('.counter').counterUp({
    delay: 10,
    time: 1000
});